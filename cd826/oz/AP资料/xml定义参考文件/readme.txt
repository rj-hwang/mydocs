AP(Application Platform)平台部署文件结构说明：
1、AP平台中应用部署以文件夹为基础，默认该文件夹的名称就是应用的名称；
2、一个文件夹中一般包含如下三个定义文件：
   1) app.xml：应用描述文件，必须有，文件名称固定为app.xml，不得修改；
   2）*.dm.xml：数据模型描述文件，必须文件，文件后缀名称必须为dm.xml。范例：clgl.dm.xml；
   3）*.ui.xml：页面模型描述文件，可选文件，文件后缀名称必须为ui.xml。范例：clgl.ui.xml。

